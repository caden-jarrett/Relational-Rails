<html>
  <head>
    <a href="/">Welcome/Navigation</a>
    <h1>Lead Actors</h1>
  </head>

  <body>
    <% @lead_actors.each do |actor| %>
      <h3><%= link_to actor.name, "/lead_actors/#{actor.id}" %></h3>
      <p>Created At: <%= actor.created_at %></p>
    <% end %>
  </body>
</html>
